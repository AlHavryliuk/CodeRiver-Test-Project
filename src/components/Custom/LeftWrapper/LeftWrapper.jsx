import { CustomLeftWrapper } from "./LeftWrapper.styles"

const LeftWrapper = ({ children }) => {
    return (
        <CustomLeftWrapper>{children}</CustomLeftWrapper>
    )
}

export default LeftWrapper